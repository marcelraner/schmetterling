#include <application.hpp>

#include <cstdint>

void Application::run()
{
    uint32_t update_event_class = 0;
    uint32_t render_event_class = 1;

    /*Window window;
    Camera camera;

    UpdateTriggerer update_triggerer(update_event_class);
    UpdateHandler update_handler(update_event_class);
    RenderTriggerer render_triggerer;
    RenderHandler render_handler(render_event_class)
    
    EventLoop event_handler(update_triggerer, render_triggerer);

    event_handler.registerOnRenderCallback(std::bind(&Renderer::render, renderer));

    event_handler.loop();*/
}
